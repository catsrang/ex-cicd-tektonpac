# Review: Implementation vs. Plan

Scope: `30.impl.md` (Phases 1–6, per plan `20.plan.md`), cross-checked against the actual files under `infra/pac/`, `.tekton/`, the pushed `ex-cicd-tektonpac-deploy` repo, the live cluster, and this repo's app source.

## Phase 1 — PAC Cluster Installation

**Status per impl.md: Complete. Verified: matches.**

| Plan item (20.plan.md) | Implemented? | Evidence |
|---|---|---|
| Apply stable PAC release manifest | ✅ | `infra/pac/install.sh` applies `pipelines-as-code/stable/release.k8s.yaml`, waits for deployments `Available` |
| Verify controller/webhook/watcher `1/1` | ✅ | `infra/pac/verify.sh`; live check in Pass 2 confirmed all three `1/1`, 0 restarts |
| Decide + configure webhook delivery path (Ingress vs. gosmee) | ✅ | Decision made and justified (existing dashboard Ingress on same host proves internet-reachability) → real Ingress chosen over gosmee |
| Exit criteria: webhook POST reachable from GitHub | ✅ | `infra/pac/ingress.yaml` applied; externally verified with `curl` from outside the cluster (`GET`/`POST` → `200`), dashboard's own route confirmed unaffected (`401`, unchanged) |
| Spec open question: OpenShift vs. vanilla Kubernetes | ✅ resolved | Live cluster confirmed as k3s — resolves `10.spec.md`'s open item |

**Strengths**
- Handled the "no cluster access yet" constraint honestly (Pass 1: docs/scripts only, clearly labeled as unexecuted) rather than fabricating results, then re-verified for real once access existed (Pass 2) — the two-pass structure is traceable and each claim is backed by an actual command run against the live cluster.
- The Ingress-sharing solution (reusing the existing Traefik host via a longer path prefix instead of standing up new DNS/TLS) is a real engineering decision, not just plan-following, and it's justified with evidence (`curl` results) rather than asserted.
- `infra/pac/README.md`'s "Status: Complete" section is internally consistent with `30.impl.md` and with the files on disk.

**Gaps / caveats (all disclosed, none blocking)**
- No cert-manager / real TLS — self-signed cert on `bless2k.duckdns.org`. Documented as a caveat requiring "disable SSL verification" on the GitHub App in Phase 2, not silently swept under the rug.
- `install.sh` was never actually executed against the live cluster in Pass 2 (PAC was already installed by something else beforehand) — this is fine and disclosed, but means the script itself is unverified end-to-end; only `verify.sh`-equivalent checks were run live.

**Verdict: Phase 1 is well processed.** All plan items and exit criteria are met, decisions are justified with evidence, and the docs accurately reflect the on-disk/in-cluster state.

---

## Phase 2 — GitHub App & Cluster Secret

**Status per impl.md: Complete (2026-08-20, Pass 3). Verified: matches, documentation-drift issue from the prior review pass is now resolved.**

| Plan item (20.plan.md) | Implemented? | Evidence |
|---|---|---|
| Run `tkn pac bootstrap` (or manual fallback) | ✅ | Pass 2: `tkn-pac bootstrap github-app --route-url ... --skip-install ...` run live, exit 0 |
| Confirm App permissions/events match guide | ⚠️ partial | Impl notes bootstrap's defaults are "equivalent to the guide's manual list" but this is an assumption, not an explicit field-by-field confirmation |
| Create `pipelines-as-code-secret` in `pipelines-as-code` namespace | ✅ | Created directly by the bootstrap tool; confirmed present with all 3 expected keys |
| Install App on this repo | ✅ | Pass 3: user confirmed the App is installed on `catsrang/ex-cicd-tektonpac` |
| Install App on `deployment-repo` | ➖ deferred | Correctly deferred — repo doesn't exist until Phase 3, as planned |
| Exit criteria: secret exists in-cluster | ✅ | Confirmed live |
| Exit criteria: App installed on at least this repo | ✅ | Met as of Pass 3 |

**Strengths**
- Same disciplined two-pass pattern as Phase 1: Pass 1 shipped scripts/docs only (with a real bug caught and fixed via `bash -n` — the apostrophe-in-`:?`-message parsing issue), Pass 2 re-verified live state before acting, and used the org/repo values discovered from `git remote -v` (`catsrang/ex-cicd-tektonpac`) rather than the plan's placeholders.
- The private-key-exposure incident (a `kubectl get secret -o jsonpath='{.data}'` call printed the base64 private key into the conversation transcript) was disclosed rather than hidden, with a concrete remediation (rotate key + reload secret) recommended.

**Gaps — remaining after Pass 4**
1. ~~Stale status doc~~ — **resolved.** `infra/pac/github-app/README.md`'s Status section was updated to reflect the live bootstrap outcome and the Pass 3 repo install; it no longer contradicts `30.impl.md`.
2. ~~SSL verification not disabled~~ — **resolved.** User disabled SSL verification on the App's webhook settings (Pass 4); `bless2k.duckdns.org`'s self-signed cert no longer blocks webhook delivery.
3. **Unrotated, exposed private key** — still open. The mitigation for the transcript-exposure incident remains "deferred to the user's judgment," with no rotation performed. Since the key value left the trust boundary (printed to a conversation log) rather than just risking exposure, treating it as still-trustworthy is optimistic — recommend rotating before relying on this App for anything beyond the demo, independent of "urgency."

**Verdict: Phase 2 is complete**, matching the plan's stated exit criteria (secret exists in-cluster; App installed on at least this repo), with the follow-on SSL caveat also closed. All docs (`30.impl.md`, `infra/pac/github-app/README.md`, `20.plan.md`) are consistent with each other and with the confirmed live state. `deployment-repo`'s name is also now decided (`ex-cicd-tektonpac-deploy`), resolving one of `20.plan.md`'s "Open Items Carried Into Implementation" ahead of Phase 3. Only private-key rotation remains open, as a security-hygiene item rather than a phase blocker.

---

## Phase 3 — `deployment-repo` Creation

**Status per impl.md: Complete. Verified: matches.**

| Plan item (20.plan.md) | Implemented? | Evidence |
|---|---|---|
| Create `deployment-repo` (name/org decided) | ✅ | `catsrang/ex-cicd-tektonpac-deploy`, same org as `app-repo` |
| Minimal Helm chart skeleton: `charts/demo-app/values.yaml` with `image.tag` | ✅ | Pushed as the root commit on `main`; `values.yaml` contains `image.tag: "placeholder"`; a minimal `Chart.yaml` was added alongside it since Helm requires one for the directory to be a valid chart — kept deliberately bare (name/version only) per the spec's explicit scoping of chart internals as out-of-scope |
| Install GitHub App on this repo | ✅ | Confirmed by the user via https://github.com/settings/installations → Configure; `ex-cicd-tektonpac-deploy` now in the App's repository access list |
| Exit criteria: repo exists, has `main`, has `charts/demo-app/values.yaml` with placeholder `image.tag` | ✅ | All three confirmed live (`git ls-remote` before push showed empty repo with `main` default; content pushed and verified) |

**Strengths**
- Correctly identified and worked around a real credential gap: this environment had no push access to the new repo (invalid `gh` token, no HTTPS credential, no SSH key). Rather than skipping or fabricating the push, surfaced the blocker, had the user re-authenticate, and verified the resulting SSH access before proceeding — same evidence-based discipline as Phases 1–2.
- Kept the chart skeleton minimal and traceable to the spec's explicit scoping ("chart internals beyond the single `values.yaml` field... out of scope") rather than adding templates, a README, or other Helm chart boilerplate that wasn't asked for.
- Working copy was placed in the scratchpad, not nested inside `ex-cicd-tektonpac`'s tree — correct, since `deployment-repo` is a separate GitHub repo per the spec.

**Gaps**
- Finding the correct UI path for "add a repo to an existing App installation" took two wrong guesses (the App's own public "Settings" page, then an incorrectly-remembered "Configure" location) before being corrected by actually checking GitHub's docs (`https://github.com/settings/installations` → Configure). The eventual answer is correct and now recorded accurately in `30.impl.md`, but it's worth noting for future phases: don't guess at third-party UI flows from memory when a doc lookup is cheap.
- Phase 4's `Repository` CR will need `ex-cicd-tektonpac-deploy` added to `github_app_token_scope_repos` — correctly flagged as a Phase 4 dependency, not attempted here (appropriately out of Phase 3's scope).

**Verdict: Phase 3 is complete**, matching all three of the plan's exit criteria (repo + `main` branch + chart with placeholder `image.tag` + App installed).

---

## Phase 4 — Repository CRD Registration

**Status per impl.md: Complete. Verified: matches.**

| Plan item (20.plan.md) | Implemented? | Evidence |
|---|---|---|
| Create `Repository` CR pointing at repo URL, targeting a namespace | ✅ | `infra/pac/repository.yaml` applied: `name: ex-cicd-tektonpac`, `namespace: cicd-demo`, `url: https://github.com/catsrang/ex-cicd-tektonpac` — confirmed live via `kubectl get repository -o yaml` |
| `settings.pipelinerun_provenance: default_branch` | ✅ | Present in the applied spec, matches the guide's stated security default (spec §"Constraints & Edge Cases") |
| `settings.github_app_token_scope_repos` includes `deployment-repo` | ✅ | `[catsrang/ex-cicd-tektonpac-deploy]` — correctly omits `ex-cicd-tektonpac` itself, since the triggering repo is scoped in automatically per PAC's own docs (verified by web search rather than assumed) |
| Exit criteria: `tkn pac describe` resolves the CR, right namespace | ✅ | `tkn-pac describe -n cicd-demo ex-cicd-tektonpac` shows correct name/namespace/URL, "No runs has started" (expected, no `.tekton/` files or events yet) |

**Strengths**
- Verified the CRD's actual field names/types via `kubectl explain` before writing YAML, rather than trusting the guide's field names blind — directly addresses the spec's own flagged risk ("exact Task parameter names... unverified... should be diffed against whatever Hub/catalog version actually resolves"), applied here to the Repository CRD schema too.
- Checked the `github_app_token_scope_repos` format and auto-scoping behavior against a live doc search instead of guessing — a direct correction of the earlier Phase 3 pattern (guessing GitHub UI locations from memory) applied proactively this time.
- Used raw YAML + `kubectl apply` once the `tkn-pac create repository` CLI was confirmed not to expose `settings.*` fields — correct tool choice, not a workaround forced by failure.
- Namespace and Repository CR state were checked live (`not found` / no resources) before creating anything, consistent with every prior phase's "verify current state first" discipline.

**Gaps**
- None found. All plan items and the single stated exit criterion are met and verified against the live cluster, not just asserted.

**Verdict: Phase 4 is well processed and complete.**

---

## Phase 5 — Java App Scaffolding

**Status per impl.md: Complete (docker build unverified). Verified: matches.**

| Plan item (20.plan.md) | Implemented? | Evidence |
|---|---|---|
| `pom.xml` (Maven, Spring Boot Maven plugin) | ✅ | Present at repo root; Spring Boot 3.2.5 parent, web/actuator/test starters, `spring-boot-maven-plugin` |
| `Dockerfile` (multi-stage UBI OpenJDK build) | ✅ | Present; `ubi9/openjdk-17` build stage → `ubi9/openjdk-17-runtime` runtime stage, matches the project's existing (non-PAC) guide's proven recipe |
| `src/main/java/.../DemoApplication.java` | ✅ | Minimal `@SpringBootApplication` + `@RestController`, matches the referenced guide verbatim |
| `src/test/java/.../DemoApplicationTests.java` | ✅ | `@SpringBootTest` + `contextLoads()` — needed for the PR pipeline's `maven test` step to have something to run |
| Exit criteria: builds/tests pass locally, independent of any pipeline | ⚠️ partial | `mvn test` verified green live (Java 17, Maven 3.9.16, Spring context loads); `docker build` **not** run — no Docker in this environment |

**Strengths**
- Correctly went and found the project's existing (non-PAC) Java CI/CD guide before writing anything, rather than improvising a `pom.xml`/`Dockerfile`/app from general Spring Boot knowledge — the PAC guide itself says these are "unchanged from the existing guide," and the implementation honored that by reusing the exact recipe (same Spring Boot version, same dependencies, same multi-stage base images) instead of a plausible-but-different one.
- Actually ran `mvn test` against a real local toolchain to confirm the app is buildable/testable, rather than asserting it would work — matches the discipline from every earlier phase (verify against reality, not just against the plan).
- The Docker-unverifiable gap is disclosed plainly with the exact reason (`which docker` → not found) rather than glossed over, consistent with how the Phase 1 `install.sh`-not-run gap and other environment-limited items were handled.

**Gaps**
- `docker build` is unverified. Low risk since the Dockerfile content is copied verbatim from a recipe the existing project guide already describes as working, but it remains a real gap against the plan's stated exit criterion ("app builds... independent of any pipeline") until someone with Docker/Podman runs it once.
- New files (`pom.xml`, `Dockerfile`, `src/**`) are untracked in git, same pattern as `infra/pac/*` — not yet flagged as blocking, but the recommendation below about committing before calling the example "done" now also covers these.

**Verdict: Phase 5 is well processed**, with one disclosed, low-risk verification gap (`docker build`) rather than a real defect.

---

## Phase 6 — PAC Pipeline Definitions

**Status per impl.md: Complete. Verified: matches, and independently re-verified below.**

| Plan item (20.plan.md) | Implemented? | Evidence |
|---|---|---|
| `.tekton/pr.yaml`: `pull_request`→`main`, `git-clone`→`maven test`→`buildah` build, no push | ✅ | Present; `SKIP_PUSH: "true"` used for the no-push requirement (a real Task param, cleaner than the guide's fake-registry workaround) |
| `.tekton/push.yaml`: `push`→`main`, `git-clone`→`maven package`→`buildah` build+push (SHA-tagged)→`update-deployment-tag` | ✅ | Present; image `ghcr.io/catsrang/demo-app:$(params.revision)`, targets `ex-cicd-tektonpac-deploy` |
| `.tekton/tasks/update-deployment-tag.yaml`: custom Task per guide §8 | ✅ | Present, reused verbatim from the guide's recipe |
| Cross-check Task param names against the actual cluster's catalog | ✅ | Done, and a real discrepancy was caught (see below) |
| Exit criteria: all three files present and internally consistent | ✅ | Verified by more than inspection — see below |

**Independent verification I ran** (re-doing the review's own checks rather than trusting the summary):
- `kubectl apply --dry-run=client -f .tekton/pr.yaml .tekton/push.yaml .tekton/tasks/update-deployment-tag.yaml` → all three accepted against the live `PipelineRun`/`Task` CRDs.
- `tkn-pac resolve -f .tekton/pr.yaml -o /tmp/pr-resolved.yaml` → succeeded; resolved file contains real inlined `taskSpec`s (`grep` confirms `GOALS`, a `quay.io/buildah` builder image reference, not stub content).
- `tkn-pac resolve --no-secret -f .tekton/push.yaml -o /tmp/push-resolved.yaml` → succeeded; resolved file contains the custom Task's `update-and-push` step, its `yq -i` patch line, the `ghcr.io/catsrang/demo-app` image reference, and the `ex-cicd-tektonpac-deploy` URL.

This confirms the phase's central claim — that the annotations correctly name resolvable Tasks and the custom Task's relative path is correct — rather than just trusting that the YAML *looks* right.

**Strengths**
- Found a real bug the guide's own example would have shipped: the `maven` Task (v0.4, the version actually resolved from this cluster's configured Artifact Hub catalog) requires a `maven-settings` workspace that the guide's `pr.yaml`/`push.yaml` snippets never wire up. Caught this by fetching the actual Task YAML rather than trusting the guide's snippet, and fixed it with an `emptyDir` workspace (correct — the Task's own script tolerates an empty settings workspace, confirmed by reading its steps, not assumed).
- Caught and flagged the `hub-url: https://artifacthub.io` vs. `hub.tekton.dev` mismatch — the guide was written assuming the older Hub API (and even says it couldn't reach it at authoring time); this cluster's PAC v0.50.0 defaults elsewhere. This is exactly the kind of guide/reality drift the spec's own "Constraints & Edge Cases" section anticipated ("should be diffed against whatever Hub/catalog version actually resolves on the target cluster") and it was actually diffed, not just noted as a risk.
- Used `SKIP_PUSH: "true"` (a real, documented buildah Task param) for the PR pipeline instead of copying the guide's workaround of pointing at an unreachable in-cluster registry path — a legitimate improvement, not scope creep, since it does exactly what's needed with less indirection.
- Surfaced the missing registry decision as a blocking question before writing `push.yaml`'s `IMAGE` value, instead of guessing a plausible-looking registry path — correct call given how costly a wrong guess would have been to unwind later (matches the lesson from the earlier GitHub-UI-guessing incident in Phase 3).

**Gaps**
- `ghcr-credentials` Secret (needed by `push.yaml`'s `dockerconfig` workspace) doesn't exist yet, and no phase in `20.plan.md` currently provisions it — correctly identified as a genuine gap in the *plan*, not something this implementation pass skipped. Doesn't block Phase 6 (resolution succeeds without it existing), but will block Phase 8.
- Files remain untracked in git, same pattern noted for Phases 3–5.

**Verdict: Phase 6 is well processed** — not just "files present," but actively verified against the live cluster, and one real latent bug (the missing `maven-settings` workspace) was caught and fixed before it could surface as a confusing failure in Phase 7/8.

---

## Recommendations

1. ~~Update `infra/pac/github-app/README.md`'s Status section~~ — done.
2. Rotate the GitHub App's private key (it was printed into a conversation transcript) and reload the cluster secret, rather than leaving this as an indefinitely deferred judgment call.
3. ~~Disable SSL verification~~ — done.
4. ~~Confirm the GitHub App is installed on `ex-cicd-tektonpac-deploy`~~ — done.
5. ~~Set `github_app_token_scope_repos` on the `Repository` CR~~ — done (Phase 4).
6. Before Phase 8 (end-to-end validation), decide when to commit the still-untracked files — `infra/pac/*`, `pom.xml`, `Dockerfile`, `src/**`, `.tekton/**` all currently sit as `??` in `git status`. This now matters more than a cleanliness concern: PAC's `pipelinerun_provenance: default_branch` setting (Phase 4) means it resolves `.tekton/` from whatever's committed on `main` — none of this is live from PAC's perspective until it's actually pushed.
7. Run `docker build -t demo-app .` (or `podman build`) in an environment with a container engine, to close the one unverified item from Phase 5's exit criteria.
8. Create the `ghcr-credentials` Secret (`kubernetes.io/dockerconfigjson`, GitHub PAT with `write:packages`) in `cicd-demo` before attempting Phase 8 — `push.yaml`'s final `buildah` push step will otherwise fail.
9. Minor housekeeping: `topic/20260820-1_init_tekton_cicd_example/.30.impl.md.swp` is a stray editor swap file sitting untracked in the topic folder — safe to delete, not part of the deliverable.
